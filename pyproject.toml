[project]
name = "kabeljaugame"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jobl0ck", email = "jonas@dieselkraft.net" }
]
requires-python = ">=3.14"
dependencies = [
    "fpdf2>=2.8.8",
]

[project.scripts]
kabeljaugame = "kabeljaugame:main"

[build-system]
requires = ["uv_build>=0.11.25,<0.12.0"]
build-backend = "uv_build"
